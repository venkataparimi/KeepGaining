import { BrokerHub } from "@/components/broker/broker-hub";

export default function BrokersPage() {
    return (
        <main className="min-h-screen bg-background">
            <BrokerHub />
        </main>
    );
}
